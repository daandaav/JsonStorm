#![allow(non_snake_case)]
#![allow(unused_imports)]
#![forbid(unsafe_code)]

use std::{
	prelude::v1::*,
	
	io::prelude::*,
	
	error::Error,

	thread,
};

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(1 + 0, 1);
	}
}
